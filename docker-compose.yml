services:
  finagent:
    build: .
    image: finance-agent:latest
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      STREAMLIT_SERVER_HEADLESS: "true"
    restart: unless-stopped
